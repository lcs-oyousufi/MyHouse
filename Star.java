
/**
 * Write a description of class star here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class Star
{
    // instance variables - replace the example below with your own
    private Triangle star1;
    private Triangle star2;

    /**
     * Constructor for objects of class star
     * 
     * @param x     the horizontal position of the star
     * @param y     the vertical position of the star
     */
    public Star(int x, int y)
    {
        // instantiate triangles 
        star1 = new Triangle();
        
        // initialise instance variables
        drawStar(x, y);
    }

 /**
     * Draws the star
     *
     * @param x     the horizontal position of the star
     * @param y     the vertical position of the star
     */
    public void drawStar(int x, int y)
    {

    }
}
